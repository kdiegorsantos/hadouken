---
- name: Copying hadouken.py script
  copy: src=/etc/ansible/files/bin/hadouken.py dest=/usr/local/sbin/hadouken.py mode=0775 owner=root

- name: Executing hadouken.py
  command: /usr/local/sbin/hadouken.py

- name: Fetching hadouken json file
  fetch: src=/var/tmp/{{ ansible_hostname }}.json dest=/etc/ansible/files/json/ flat=yes

- name: Updating database
  local_action: command /etc/ansible/files/bin/db.py
